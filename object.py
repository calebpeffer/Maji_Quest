# Object Class
# Initializes an Object
class Object():
    def __init__(self, name:str(), description:str(), interactable:bool(), interactions:dict()):
        self.name = name
        self.description = description
        self.interactable = interactable
        self.interactions = interactions
            
  